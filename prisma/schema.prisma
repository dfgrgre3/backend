generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["partialIndexes"]
}

datasource db {
  provider = "postgresql"
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ABExperiment {
  id          String    @id @db.Uuid
  name        String
  description String?
  status      String?   @default("DRAFT")
  variants    Json?
  traffic_pct BigInt?   @default(100)
  start_date  DateTime? @db.Timestamptz(6)
  end_date    DateTime? @db.Timestamptz(6)
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Achievement {
  id              String               @id @db.Uuid
  key             String
  title           String
  description     String?
  icon            String?
  rarity          String               @default("common")
  xp_reward       BigInt?              @default(0)
  requirements    String
  is_secret       Boolean              @default(false)
  unlocked_count  Int                  @default(0)
  created_at      DateTime?            @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?            @db.Timestamptz(6)
  category        AchievementCategory?
  difficulty      Difficulty?
  deleted_at      DateTime?            @db.Timestamptz(6)
  criteria        String?
  UserAchievement UserAchievement[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Addon {
  id          String    @id
  name        String
  nameAr      String?
  description String?
  price       Float
  type        AddonType
  value       Int
  isActive    Boolean   @default(true)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AiChatMessage {
  id        String   @id @db.Uuid
  userId    String   @db.Uuid
  role      String
  content   String
  sentiment String?
  metadata  Json?
  createdAt DateTime @default(now())
  updatedAt DateTime
  User      User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_AiChatMessage_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AiGeneratedContent {
  id        String   @id @db.Uuid
  userId    String   @db.Uuid
  type      String
  title     String
  content   String
  subjectId String?  @db.Uuid
  metadata  Json?
  isUsed    Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime
  User      User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_AiGeneratedContent_user")
  Subject   Subject? @relation(fields: [subjectId], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_AiGeneratedContent_subject")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AiGeneratedExam {
  id         String       @id @db.Uuid
  userId     String       @db.Uuid
  subjectId  String       @db.Uuid
  title      String
  duration   Int
  year       Int?
  createdAt  DateTime     @default(now())
  updatedAt  DateTime
  difficulty Difficulty   @default(MEDIUM)
  User       User         @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_AiGeneratedExam_user")
  Subject    Subject      @relation(fields: [subjectId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_AiGeneratedExam_subject")
  AiQuestion AiQuestion[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AiQuestion {
  id              String          @id @db.Uuid
  examId          String          @db.Uuid
  question        String
  correctAnswer   String
  explanation     String?
  points          Int             @default(1)
  order           Int             @default(0)
  createdAt       DateTime        @default(now())
  updatedAt       DateTime
  options         Json?
  AiGeneratedExam AiGeneratedExam @relation(fields: [examId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_AiQuestion_exam")
  user            User?           @relation(fields: [userId], references: [id])
  userId          String?         @db.Uuid
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AnalyticsEvent {
  id           String    @id @db.Uuid
  event_id     String    @unique @db.VarChar(255)
  event_type   String    @db.VarChar(100)
  user_id      String?   @db.Uuid
  payload      Json      @default("{}")
  source       String?   @default("frontend") @db.VarChar(50)
  ip_address   String?   @db.VarChar(50)
  user_agent   String?
  received_at  DateTime  @default(now()) @db.Timestamptz(6)
  processed_at DateTime? @db.Timestamptz(6)
  createdAt    DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  User         User?     @relation(fields: [user_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_AnalyticsEvent_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Announcement {
  id        String   @id @db.Uuid
  title     String
  content   String
  type      String   @default("INFO")
  priority  Int      @default(0)
  isActive  Boolean  @default(true)
  authorId  String?  @db.Uuid
  createdAt DateTime @default(now())
  updatedAt DateTime
  User      User?    @relation(fields: [authorId], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_Announcement_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AuditLog {
  id          String    @id @db.Uuid
  user_id     String?   @db.Uuid
  event_type  String
  action      String?
  resource    String?
  resource_id String?
  changes     String?
  metadata    Json?
  ip_address  String?
  user_agent  String?
  device_info String?
  location    String?
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  User        User?     @relation(fields: [user_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_AuditLog_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Automation {
  id          String    @id @db.Uuid
  name        String
  description String?
  event       String
  trigger     Json?
  conditions  Json?
  actions     Json?
  is_active   Boolean?  @default(true)
  last_run_at DateTime? @db.Timestamptz(6)
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AutomationLog {
  id             String         @id @db.Uuid
  ruleId         String         @db.Uuid
  userId         String         @db.Uuid
  actionTaken    String
  metadata       Json?
  createdAt      DateTime       @default(now())
  User           User           @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_AutomationLog_user")
  AutomationRule AutomationRule @relation(fields: [ruleId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_AutomationLog_rule")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AutomationRule {
  id            String          @id @db.Uuid
  name          String
  description   String?
  triggerType   String
  conditions    Json?
  actionType    String
  actionData    Json?
  isActive      Boolean         @default(true)
  createdAt     DateTime        @default(now())
  updatedAt     DateTime
  AutomationLog AutomationLog[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model BiometricChallenge {
  id        String   @id @db.Uuid
  challenge String
  type      String
  userId    String?  @db.Uuid
  expiresAt DateTime
  used      Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime
  User      User?    @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_BiometricChallenge_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model BiometricCredential {
  id           String   @id @db.Uuid
  userId       String   @db.Uuid
  credentialId String   @unique
  publicKey    String
  counter      Int      @default(0)
  deviceType   String?
  deviceName   String?
  transports   Json?
  createdAt    DateTime @default(now())
  updatedAt    DateTime
  User         User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_BiometricCredential_user")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model BlogPost {
  id          String          @id @db.Uuid
  title       String
  content     String?
  excerpt     String?
  slug        String          @unique
  views       Int             @default(0)
  status      BlogPostStatus?
  image       String?
  tags        Json?
  author_id   String?         @db.Uuid
  category_id String?         @db.Uuid
  deleted_at  DateTime?       @db.Timestamptz(6)
  publishedAt DateTime?       @map("published_at") @db.Timestamptz(6)
  createdAt   DateTime?       @map("created_at") @db.Timestamptz(6)
  updatedAt   DateTime?       @map("updated_at") @db.Timestamptz(6)
  User        User?           @relation(fields: [author_id], references: [id], onUpdate: NoAction, map: "fk_BlogPost_author")
  Category    Category?       @relation(fields: [category_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_BlogPost_category")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Book {
  id           String         @id @db.Uuid
  title        String
  author       String?
  description  String?
  rating       Float          @default(0)
  views        Int            @default(0)
  downloads    Int            @default(0)
  created_at   DateTime       @default(now())
  updated_at   DateTime
  tags         String[]       @default([])
  price        Float?         @default(0)
  deleted_at   DateTime?      @db.Timestamptz(6)
  cover_url    String?
  download_url String?
  subject_id   String?        @db.Uuid
  is_free      Boolean?       @default(true)
  Subject      Subject?       @relation(fields: [subject_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_Book_subject")
  BookProgress BookProgress[]
  BookReview   BookReview[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model BookProgress {
  id          String   @id @db.Uuid
  userId      String   @db.Uuid
  bookId      String   @db.Uuid
  progress    Float    @default(0)
  lastReadAt  DateTime @default(now())
  isCompleted Boolean  @default(false)
  currentPage Int      @default(0)
  totalPages  Int?
  createdAt   DateTime @default(now())
  updatedAt   DateTime
  User        User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_BookProgress_user")
  Book        Book     @relation(fields: [bookId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_BookProgress_book")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model BookReview {
  id        String   @id @db.Uuid
  bookId    String   @db.Uuid
  userId    String   @db.Uuid
  rating    Int
  comment   String?
  createdAt DateTime @default(now())
  updatedAt DateTime
  User      User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_BookReview_user")
  Book      Book     @relation(fields: [bookId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_BookReview_book")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Category {
  id          String        @id @db.Uuid
  name        String
  description String?
  slug        String        @unique
  icon        String?
  type        CategoryType?
  deleted_at  DateTime?     @db.Timestamptz(6)
  createdAt   DateTime?     @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt   DateTime?     @map("updated_at") @db.Timestamptz(6)
  BlogPost    BlogPost[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Challenge {
  id                  String                @id @db.Uuid
  title               String
  description         String?
  type                String?               @default("daily")
  category            String?
  xp_reward           BigInt?               @default(0)
  requirements        String
  start_date          DateTime?             @db.Timestamptz(6)
  end_date            DateTime?             @db.Timestamptz(6)
  is_active           Boolean               @default(true)
  subject_id          String?               @db.Uuid
  levelRange          String?
  created_at          DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?             @db.Timestamptz(6)
  difficulty          Difficulty?
  deleted_at          DateTime?             @db.Timestamptz(6)
  Subject             Subject?              @relation(fields: [subject_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_Challenge_subject")
  UserChallenge       UserChallenge[]
  ChallengeCompletion ChallengeCompletion[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ChallengeCompletion {
  id          String    @id @db.Uuid
  userId      String    @db.Uuid
  challengeId String    @db.Uuid
  completedAt DateTime  @default(now())
  progress    Float     @default(0)
  isCompleted Boolean   @default(false)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime
  User        User      @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ChallengeCompletion_user")
  Challenge   Challenge @relation(fields: [challengeId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ChallengeCompletion_challenge")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ContentPreference {
  id        String   @id @db.Uuid
  userId    String   @db.Uuid
  itemType  String
  itemValue String
  weight    Float    @default(1.0)
  source    String?
  updatedAt DateTime
  createdAt DateTime @default(now())
  User      User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ContentPreference_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ContentReport {
  id            String               @id @db.Uuid
  reporter_id   String               @db.Uuid
  content_type  String               @db.VarChar(50)
  content_id    String               @db.Uuid
  content_title String?              @db.VarChar(500)
  reason        String               @db.Text
  description   String
  status        ContentReportStatus?
  resolved_by   String?              @db.Uuid
  resolved_at   DateTime?            @db.Timestamptz(6)
  createdAt     DateTime             @default(now()) @map("created_at")
  updatedAt     DateTime             @map("updated_at")
  Reporter      User                 @relation("ContentReport_reporter", fields: [reporter_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ContentReport_reporter")
  Resolver      User?                @relation("ContentReport_resolver", fields: [resolved_by], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_ContentReport_resolver")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Contest {
  id                 String            @id @db.Uuid
  title              String
  description        String?
  imageUrl           String?
  category           String?
  tags               String[]
  startDate          DateTime
  endDate            DateTime
  rules              String?
  prizes             String?
  isActive           Boolean           @default(true)
  status             ContestStatus     @default(DRAFT)
  createdAt          DateTime          @default(now())
  updatedAt          DateTime
  pinCode            String?
  organizerId        String?           @db.Uuid
  deleted_at         DateTime?         @db.Timestamptz(6)
  questions_count    Int?              @default(0)
  participants_count Int?              @default(0)
  ContestQuestion    ContestQuestion[]
  User               User?             @relation("Contest_organizer", fields: [organizerId], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_Contest_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ContestQuestion {
  id            String   @id @db.Uuid
  contestId     String   @db.Uuid
  question      String
  correctAnswer String
  questionOrder Int      @default(0) @map("order")
  options       Json?
  duration      Int      @default(30)
  points        Int      @default(100)
  createdAt     DateTime @default(now())
  updatedAt     DateTime
  Contest       Contest  @relation(fields: [contestId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ContestQuestion_contest")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Coupon {
  id               String       @id @db.Uuid
  code             String       @unique
  description      String?
  discount_type    DiscountType @default(PERCENTAGE)
  discount_value   Decimal      @db.Decimal
  max_uses         BigInt?
  used_count       Int          @default(0)
  expiryDate       DateTime?
  is_active        Boolean      @default(true)
  minOrderAmount   Float?       @default(0)
  created_at       DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?    @db.Timestamptz(6)
  isWinBack        Boolean?     @default(false)
  minInactiveDays  Int?
  regionTarget     String?
  schoolTarget     String?
  userTargetId     String?      @db.Uuid
  deleted_at       DateTime?    @db.Timestamptz(6)
  min_order_amount Decimal?     @default(0) @db.Decimal
  expiry_date      DateTime?    @db.Timestamptz(6)
  User             User?        @relation(fields: [userTargetId], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_Coupon_user")
  Payment          Payment[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model CourseReview {
  id         String    @id @db.Uuid
  rating     BigInt    @default(5)
  comment    String?
  subject_id String    @db.Uuid
  user_id    String    @db.Uuid
  deleted_at DateTime? @db.Timestamptz(6)
  isVisible  Boolean?  @default(true) @map("is_visible")
  createdAt  DateTime? @map("created_at") @db.Timestamptz(6)
  updatedAt  DateTime? @map("updated_at") @db.Timestamptz(6)
  User       User      @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_CourseReview_user")
  Subject    Subject   @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_CourseReview_subject")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model CustomGoal {
  id           String    @id @db.Uuid
  userId       String    @db.Uuid
  title        String
  description  String?
  targetValue  Float
  currentValue Float     @default(0)
  unit         String
  category     String
  isCompleted  Boolean   @default(false)
  createdAt    DateTime  @default(now())
  completedAt  DateTime?
  xpReward     Int       @default(10)
  User         User      @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_CustomGoal_user")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Event {
  id              String          @id @db.Uuid
  title           String
  description     String?
  startDate       DateTime
  endDate         DateTime
  location        String?
  maxAttendees    Int?
  imageUrl        String?
  organizerId     String          @db.Uuid
  category        String
  isPublic        Boolean         @default(true)
  createdAt       DateTime        @default(now())
  updatedAt       DateTime
  tags            String[]        @default([])
  deleted_at      DateTime?       @db.Timestamptz(6)
  type            String?         @default("workshop")
  start_date      DateTime?       @db.Timestamptz(6)
  end_date        DateTime?       @db.Timestamptz(6)
  is_online       Boolean?        @default(true)
  attendees_count Int?            @default(0)
  is_active       Boolean?        @default(true)
  EventAttendee   EventAttendee[]
  Organizer       User            @relation(fields: [organizerId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_Event_organizer")

  @@index([is_active], map: "idx_event_is_active", where: raw("(deleted_at IS NULL)"))
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model EventAttendee {
  id        String   @id @db.Uuid
  event_id  String   @db.Uuid
  user_id   String   @db.Uuid
  status    String   @default("pending")
  createdAt DateTime @default(now())
  updatedAt DateTime
  Event     Event    @relation(fields: [event_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_event_attendee_event")
  User      User     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_event_attendee_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Exam {
  id          String       @id @db.Uuid
  subject_id  String       @db.Uuid
  title       String
  year        Int
  url         String
  type        ExamType?
  created_at  DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at  DateTime?    @db.Timestamptz(6)
  duration    BigInt?
  max_score   Decimal?     @default(100) @db.Decimal
  deleted_at  DateTime?    @db.Timestamptz(6)
  description String?
  difficulty  Difficulty?
  is_active   Boolean?     @default(true)
  Subject     Subject      @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_Exam_subject")
  ExamResult  ExamResult[]
  Question    Question[]
  SubTopic    SubTopic[]
  testResults TestResult[]
}

/// This table is a partition table and requires additional setup for migrations. Visit https://pris.ly/d/partition-tables for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ExamResult {
  id         String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id    String    @db.Uuid
  exam_id    String    @db.Uuid
  score      Decimal?  @default(0) @db.Decimal
  max_score  Float?    @default(0)
  passed     Boolean?  @default(false)
  answers    Json?
  taken_at   DateTime  @default(now()) @db.Timestamptz(6)
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_at DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at DateTime? @db.Timestamptz(6)
  Exam       Exam      @relation(fields: [exam_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_exam_results_exam_id")
  User       User      @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_exam_results_user_id")

  @@id([id, taken_at])
  @@index([user_id, taken_at(sort: Desc)], map: "idx_exam_result_user_taken_active", where: raw("(deleted_at IS NULL)"))
  @@index([user_id, taken_at(sort: Desc)], map: "idx_exam_result_user_taken_safe", where: raw("(deleted_at IS NULL)"))
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ForumCategory {
  id          String       @id @db.Uuid
  name        String
  description String?
  icon        String?
  order       BigInt?      @default(0)
  created_at  DateTime?    @db.Timestamptz(6)
  updated_at  DateTime?    @db.Timestamptz(6)
  deleted_at  DateTime?    @db.Timestamptz(6)
  ForumPost   ForumPost[]
  ForumTopic  ForumTopic[]

  @@index([order, created_at(sort: Desc)], map: "idx_forum_category_order_active", where: raw("(deleted_at IS NULL)"))
  @@index([order, created_at(sort: Desc)], map: "idx_forum_category_order_safe", where: raw("(deleted_at IS NULL)"))
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ForumPost {
  id         String        @id @db.Uuid
  title      String
  content    String
  categoryId String        @db.Uuid
  authorId   String        @db.Uuid
  isPinned   Boolean       @default(false)
  isLocked   Boolean       @default(false)
  createdAt  DateTime      @default(now())
  updatedAt  DateTime
  views      Int           @default(0)
  Author     User          @relation("ForumPost_author", fields: [authorId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ForumPost_user")
  Category   ForumCategory @relation(fields: [categoryId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ForumPost_category")
  ForumReply ForumReply[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ForumReply {
  id        String    @id @db.Uuid
  content   String
  postId    String    @db.Uuid
  authorId  String    @db.Uuid
  createdAt DateTime  @default(now())
  updatedAt DateTime
  Author    User      @relation("ForumReply_author", fields: [authorId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ForumReply_user")
  Post      ForumPost @relation(fields: [postId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ForumReply_post")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ForumTopic {
  id          String         @id @db.Uuid
  title       String
  content     String?
  author_id   String?        @db.Uuid
  category_id String?        @db.Uuid
  views       BigInt?        @default(0)
  is_pinned   Boolean?       @default(false)
  is_locked   Boolean?       @default(false)
  created_at  DateTime?      @db.Timestamptz(6)
  updated_at  DateTime?      @db.Timestamptz(6)
  deleted_at  DateTime?      @db.Timestamptz(6)
  User        User?          @relation(fields: [author_id], references: [id], onUpdate: NoAction, map: "fk_ForumTopic_author")
  Category    ForumCategory? @relation(fields: [category_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_ForumTopic_category")

  @@index([category_id, created_at(sort: Desc)], map: "idx_forum_topic_category_created_active", where: raw("(deleted_at IS NULL)"))
  @@index([category_id, created_at(sort: Desc)], map: "idx_forum_topic_category_created_safe", where: raw("(deleted_at IS NULL)"))
  @@index([is_pinned(sort: Desc), created_at(sort: Desc), id, title, author_id, category_id, views, is_locked], map: "idx_forum_topic_pinned_created_active", where: raw("(deleted_at IS NULL)"))
}


/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Invoice {
  id             String        @id @db.Uuid
  invoice_number String?
  user_id        String?       @db.Uuid
  payment_id     String?       @db.Uuid
  amount         Decimal       @db.Decimal
  currency       String        @default("EGP")
  status         InvoiceStatus @default(DRAFT)
  issueDate      DateTime      @default(now())
  due_date       DateTime?     @db.Timestamptz(6)
  paidDate       DateTime?
  pdf_url        String?
  billingDetails Json?
  items          Json?
  created_at     DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at     DateTime      @db.Timestamptz(6)
  deleted_at     DateTime?     @db.Timestamptz(6)
  User           User?         @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_invoices_user_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model LeaderboardEntry {
  id          String   @id @db.Uuid
  userId      String   @db.Uuid
  type        String
  period      String?
  subjectId   String?  @db.Uuid
  levelRange  String?
  seasonId    String?  @db.Uuid
  totalXP     Int      @default(0)
  studyXP     Int      @default(0)
  taskXP      Int      @default(0)
  examXP      Int      @default(0)
  challengeXP Int      @default(0)
  questXP     Int      @default(0)
  rank        Int?
  level       Int      @default(1)
  lastUpdated DateTime
  User        User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_LeaderboardEntry_user")
  Subject     Subject? @relation(fields: [subjectId], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_LeaderboardEntry_subject")
  Season      Season?  @relation(fields: [seasonId], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_LeaderboardEntry_season")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model LessonAnswer {
  id             String         @id @db.Uuid
  questionId     String         @db.Uuid
  userId         String         @db.Uuid
  content        String
  isTeacher      Boolean        @default(false)
  createdAt      DateTime       @default(now())
  updatedAt      DateTime
  User           User           @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_LessonAnswer_user")
  LessonQuestion LessonQuestion @relation(fields: [questionId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_LessonAnswer_question")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model LessonAttachment {
  id           String    @id @db.Uuid
  title        String
  deleted_at   DateTime? @db.Timestamptz(6)
  sub_topic_id String?   @db.Uuid
  fileUrl      String?   @map("file_url")
  fileType     String?   @map("file_type")
  fileSize     BigInt?   @map("file_size")
  createdAt    DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt    DateTime? @map("updated_at") @db.Timestamptz(6)
  SubTopic     SubTopic? @relation(fields: [sub_topic_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_LessonAttachment_sub_topic")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model LessonNote {
  id         String   @id @db.Uuid
  userId     String   @db.Uuid
  subTopicId String   @db.Uuid
  content    String
  createdAt  DateTime @default(now())
  updatedAt  DateTime
  User       User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_LessonNote_user")
  SubTopic   SubTopic @relation(fields: [subTopicId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_LessonNote_subtopic")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model LessonQuestion {
  id           String         @id @db.Uuid
  userId       String         @db.Uuid
  subTopicId   String         @db.Uuid
  content      String
  createdAt    DateTime       @default(now())
  updatedAt    DateTime
  User         User           @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_LessonQuestion_user")
  SubTopic     SubTopic       @relation(fields: [subTopicId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_LessonQuestion_subtopic")
  LessonAnswer LessonAnswer[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model LiveEvent {
  id          String    @id @db.Uuid
  title       String
  description String?
  type        String?   @default("LIVE")
  status      String?   @default("UPCOMING")
  start_time  DateTime? @db.Timestamptz(6)
  end_time    DateTime? @db.Timestamptz(6)
  speaker     String?
  join_link   String?
  image       String?
  subject_id  String?   @db.Uuid
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
  deleted_at  DateTime? @db.Timestamptz(6)
  Subject     Subject?  @relation(fields: [subject_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_LiveEvent_subject")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model MarketingCampaign {
  id             String    @id @db.Uuid
  title          String
  content        String
  audience       String
  rewardType     String
  rewardValue    Float?
  status         String    @default("draft")
  scheduledAt    DateTime?
  sentAt         DateTime?
  deliveredCount Int       @default(0)
  openedCount    Int       @default(0)
  claimedCount   Int       @default(0)
  createdAt      DateTime  @default(now())
  updatedAt      DateTime
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Message {
  id         String   @id @db.Uuid
  senderId   String   @db.Uuid
  receiverId String   @db.Uuid
  content    String
  isRead     Boolean  @default(false)
  createdAt  DateTime @default(now())
  updatedAt  DateTime
  Sender     User     @relation("Message_sender", fields: [senderId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_Message_sender")
  Receiver   User     @relation("Message_receiver", fields: [receiverId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_Message_receiver")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model MlRecommendation {
  id          String    @id @db.Uuid
  userId      String    @db.Uuid
  itemType    String
  itemId      String
  score       Float
  algorithm   String
  reason      String?
  shownAt     DateTime?
  clickedAt   DateTime?
  completedAt DateTime?
  feedback    String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime
  User        User      @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_MlRecommendation_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Notification {
  id           String           @id @db.Uuid
  user_id      String           @db.Uuid
  title        String
  message      String
  is_read      Boolean          @default(false)
  action_url   String?
  icon         String?
  created_at   DateTime?        @default(now()) @db.Timestamptz(6)
  updated_at   DateTime?        @db.Timestamptz(6)
  type         NotificationType @default(INFO)
  link         String?
  deleted_at   DateTime?        @db.Timestamptz(6)
  category     String?          @default("GENERAL")
  priority     String?          @default("MEDIUM")
  status       String?          @default("pending")
  channels     Json?            @default("[]")
  broadcast_id String?          @db.Uuid
  actions      Json?            @default("[]")
  User         User             @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_notifications_user_id")

  @@index([category], map: "idx_notification_category")
  @@index([is_read], map: "idx_notification_is_read")
  @@index([status], map: "idx_notification_status")
  @@index([user_id, created_at(sort: Desc), id, type, title, message, is_read], map: "idx_notification_user_created_covering", where: raw("(deleted_at IS NULL)"))
  @@index([user_id, created_at(sort: Desc), id, type, title, message, is_read], map: "idx_notification_user_created_read_safe", where: raw("(deleted_at IS NULL)"))
  @@index([user_id, is_read, created_at(sort: Desc)], map: "idx_notification_user_read_created")
  @@index([user_id, created_at(sort: Desc)], map: "idx_notification_user_unread_active", where: raw("((deleted_at IS NULL) AND (is_read = false))"))
  @@index([user_id, created_at(sort: Desc)], map: "idx_notification_user_unread_safe", where: raw("((deleted_at IS NULL) AND (is_read = false))"))
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model OfflineLesson {
  id          String   @id @db.Uuid
  userId      String   @db.Uuid
  title       String
  description String?
  subjectId   String   @db.Uuid
  teacherId   String?  @db.Uuid
  location    String?
  startTime   DateTime
  endTime     DateTime
  maxStudents Int?
  price       Decimal? @db.Decimal
  createdAt   DateTime @default(now())
  updatedAt   DateTime
  Subject     Subject  @relation(fields: [subjectId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_OfflineLesson_subject")
  User        User     @relation("UserOfflineLessons", fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_OfflineLesson_user")
  Teacher     User?    @relation("TeacherOfflineLessons", fields: [teacherId], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_OfflineLesson_teacher")

  @@index([userId])
  @@index([teacherId])
  @@index([subjectId])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model PasswordHistory {
  id           String   @id @db.Uuid
  userId       String   @db.Uuid
  passwordHash String
  createdAt    DateTime @default(now())
  updatedAt    DateTime
  User         User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_PasswordHistory_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model PasswordPolicy {
  id               String   @id @db.Uuid
  role             String   @unique
  expirationDays   Int?
  minLength        Int      @default(8)
  maxLength        Int      @default(128)
  requireUppercase Boolean  @default(true)
  requireLowercase Boolean  @default(true)
  requireNumbers   Boolean  @default(true)
  requireSpecial   Boolean  @default(true)
  historyCount     Int      @default(10)
  isActive         Boolean  @default(true)
  createdAt        DateTime @default(now())
  updatedAt        DateTime
  warningDays      Int[]    @default([7, 3, 1])
  bannedPasswords  String[] @default([])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Payment {
  id                String         @id @db.Uuid
  user_id           String         @db.Uuid
  plan_id           String?        @db.Uuid
  amount            Decimal        @db.Decimal
  currency          String         @default("EGP")
  status            PaymentStatus?
  method            String
  reference         String         @unique
  paymob_order_id   BigInt?
  external_txn_id   String?
  completed_at      DateTime?      @db.Timestamptz(6)
  created_at        DateTime?      @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?      @db.Timestamptz(6)
  deleted_at        DateTime?      @db.Timestamptz(6)
  subject_id        String?        @db.Uuid
  paymentData       Json?
  errorMessage      String?
  couponId          String?        @db.Uuid
  discountAmount    Float?         @default(0)
  referralRewardId  String?        @db.Uuid
  creditAmount      Float?         @default(0)
  balanceUsed       Float?         @default(0)
  promoDiscount     Float?         @default(0)
  prorationDiscount Float?         @default(0)
  archiveReason     String?
  Subject           Subject?       @relation(fields: [subject_id], references: [id], onUpdate: NoAction, map: "fk_Payment_subject")
  User              User           @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_User_payments")
  Coupon            Coupon?        @relation(fields: [couponId], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_Payment_coupon")

  @@index([user_id, created_at(sort: Desc)], map: "idx_payment_user_created")
  @@index([user_id, created_at(sort: Desc), amount, status], map: "idx_payment_user_created_covering", where: raw("(deleted_at IS NULL)"))
  @@index([user_id, created_at(sort: Desc), amount, status], map: "idx_payment_user_created_covering_safe", where: raw("(deleted_at IS NULL)"))
  @@index([user_id, status, created_at(sort: Desc)], map: "idx_payment_user_status_active", where: raw("(deleted_at IS NULL)"))
  @@index([user_id, status, created_at(sort: Desc)], map: "idx_payment_user_status_active_safe", where: raw("(deleted_at IS NULL)"))
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model PaymentStatusLookup {
  code        String    @id @db.VarChar(20)
  label       String    @db.VarChar(50)
  description String?
  color       String?   @default("#000000") @db.VarChar(7)
  isActive    Boolean?  @default(true)
  sortOrder   Int?      @default(0)
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ProgressSnapshot {
  id                String   @id @db.Uuid
  userId            String   @db.Uuid
  date              DateTime
  totalStudyMinutes Int      @default(0)
  averageFocusScore Float    @default(0)
  completedTasks    Int      @default(0)
  streakDays        Int      @default(0)
  gradeAverage      Float?
  improvementRate   Float?
  createdAt         DateTime @default(now())
  updatedAt         DateTime
  User              User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ProgressSnapshot_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model PushToken {
  id        String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  userId    String    @map("user_id") @db.Uuid
  token     String    @unique(map: "idx_push_tokens_token") @db.VarChar(500)
  platform  String    @db.VarChar(20)
  provider  String    @db.VarChar(20)
  isActive  Boolean?  @default(true) @map("is_active")
  lastUsed  DateTime? @map("last_used") @db.Timestamptz(6)
  createdAt DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime? @default(now()) @map("updated_at") @db.Timestamptz(6)
  deletedAt DateTime? @map("deleted_at") @db.Timestamptz(6)
  User      User      @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_push_tokens_user_id")

  @@index([userId], map: "idx_push_tokens_user_id")
  @@map("push_tokens")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Quest {
  id            String          @id @db.Uuid
  chainId       String          @db.Uuid
  title         String
  description   String
  order         Int
  xpReward      Int
  requirements  String
  prerequisites String?
  isActive      Boolean         @default(true)
  createdAt     DateTime        @default(now())
  Chain         QuestChain      @relation(fields: [chainId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_Quest_chain")
  QuestProgress QuestProgress[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model QuestChain {
  id            String          @id @db.Uuid
  title         String
  description   String
  category      String
  totalQuests   Int             @default(1)
  isActive      Boolean         @default(true)
  createdAt     DateTime        @default(now())
  updatedAt     DateTime
  difficulty    Difficulty      @default(MEDIUM)
  Quest         Quest[]
  QuestProgress QuestProgress[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model QuestProgress {
  id          String     @id @db.Uuid
  userId      String     @db.Uuid
  questId     String     @db.Uuid
  chainId     String     @db.Uuid
  progress    Float      @default(0)
  isCompleted Boolean    @default(false)
  completedAt DateTime?
  startedAt   DateTime   @default(now())
  createdAt   DateTime   @default(now())
  updatedAt   DateTime
  User        User       @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_QuestProgress_user")
  Quest       Quest      @relation(fields: [questId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_QuestProgress_quest")
  QuestChain  QuestChain @relation(fields: [chainId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_QuestProgress_chain")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Question {
  id         String    @id @db.Uuid
  exam_id    String    @db.Uuid
  text       String
  type       String?   @default("MCQ")
  options    Json?
  answer     String
  deleted_at DateTime? @db.Timestamptz(6)
  Exam       Exam      @relation(fields: [exam_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_Exam_questions")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ReferralReward {
  id         String   @id @db.Uuid
  referrerId String   @db.Uuid
  referredId String   @db.Uuid
  amount     Float
  status     String   @default("PENDING")
  createdAt  DateTime @default(now())
  updatedAt  DateTime
  Referrer   User     @relation("ReferralReward_referrer", fields: [referrerId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ReferralReward_referrer")
  Referred   User     @relation("ReferralReward_referred", fields: [referredId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ReferralReward_referred")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Reminder {
  id         String    @id @db.Uuid
  user_id    String    @db.Uuid
  title      String
  message    String?
  remind_at  DateTime? @db.Timestamptz(6)
  repeat     String?
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
  type       String?   @default("STUDY")
  priority   String?   @default("MEDIUM")
  is_active  Boolean?  @default(true)
  deleted_at DateTime? @db.Timestamptz(6)
  User       User      @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_reminders_user_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Resource {
  id           String    @id @db.Uuid
  title        String
  description  String?
  url          String
  free         Boolean   @default(true)
  type         String
  source       String?
  price        Float?    @default(0)
  minPlanLevel Int       @default(0)
  subject_id   String?   @db.Uuid
  deleted_at   DateTime? @db.Timestamptz(6)
  createdAt    DateTime? @map("created_at") @db.Timestamptz(6)
  updatedAt    DateTime? @map("updated_at") @db.Timestamptz(6)
  Subject      Subject?  @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_Resource_subject")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Reward {
  id          String       @id @db.Uuid
  name        String
  title       String
  description String?
  type        String?      @default("VIRTUAL")
  rarity      String
  metadata    Json?
  isTradeable Boolean      @default(false)
  cost        BigInt?      @default(0)
  stock       BigInt?      @default(-1)
  deleted_at  DateTime?    @db.Timestamptz(6)
  image       String?      @map("image_url")
  isActive    Boolean?     @default(true) @map("is_active")
  createdAt   DateTime?    @map("created_at") @db.Timestamptz(6)
  updatedAt   DateTime?    @map("updated_at") @db.Timestamptz(6)
  UserReward  UserReward[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Schedule {
  id          String    @id @db.Uuid
  user_id     String    @db.Uuid
  name        String?
  title       String
  description String?
  startTime   DateTime
  endTime     DateTime
  subjectId   String?   @db.Uuid
  type        String    @default("study")
  color       String?
  active      Boolean   @default(true)
  plan_json   Json?
  version     Int       @default(0)
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
  deleted_at  DateTime? @db.Timestamptz(6)
  User        User      @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_schedules_user_id")
  Subject     Subject?  @relation(fields: [subjectId], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_Schedule_subject")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Season {
  id                  String                @id @db.Uuid
  name                String
  description         String?
  rewards             String?
  title               String
  startDate           DateTime              @map("start_date")
  endDate             DateTime              @map("end_date")
  isActive            Boolean               @default(true) @map("is_active")
  createdAt           DateTime              @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt           DateTime              @map("updated_at") @db.Timestamptz(6)
  deletedAt           DateTime?             @map("deleted_at") @db.Timestamptz(6)
  SeasonParticipation SeasonParticipation[]
  LeaderboardEntry    LeaderboardEntry[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SeasonParticipation {
  id        String   @id @db.Uuid
  userId    String   @db.Uuid
  seasonId  String   @db.Uuid
  seasonXP  Int      @default(0)
  rank      Int?
  joinedAt  DateTime @default(now())
  createdAt DateTime @default(now())
  updatedAt DateTime
  User      User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_SeasonParticipation_user")
  Season    Season   @relation(fields: [seasonId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_SeasonParticipation_season")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SecurityAuditLog {
  id        String    @id @db.Uuid
  eventType String    @map("event_type")
  userId    String?   @map("user_id") @db.Uuid
  ipAddress String?   @map("ip_address")
  userAgent String?   @map("user_agent")
  details   Json?
  severity  String?   @default("info")
  status    String?   @default("unread")
  createdAt DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  User      User?     @relation(fields: [userId], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_security_audit_logs_user_id")

  @@map("security_audit_logs")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SecurityLog {
  id         String    @id @db.Uuid
  user_id    String?   @db.Uuid
  event_type String
  ip         String
  user_agent String?
  deviceInfo String?
  location   String?
  metadata   Json?
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  updated_at DateTime? @db.Timestamptz(6)
  deleted_at DateTime? @db.Timestamptz(6)
  User       User?     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_security_logs_user_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SecurityQuestion {
  id         String   @id @db.Uuid
  userId     String   @db.Uuid
  question   String
  answerHash String
  order      Int      @default(0)
  createdAt  DateTime @default(now())
  updatedAt  DateTime
  User       User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_SecurityQuestion_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SentimentAnalysis {
  id         String   @id @db.Uuid
  userId     String   @db.Uuid
  text       String
  sentiment  String
  score      Float
  confidence Float
  emotions   Json?
  context    String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime
  User       User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_SentimentAnalysis_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model StudySession {
  id           String         @id @db.Uuid
  user_id      String         @db.Uuid
  subject_id   String?        @db.Uuid
  taskId       String?        @db.Uuid
  start_time   DateTime?      @db.Timestamptz(6)
  end_time     DateTime?      @db.Timestamptz(6)
  duration_min Int?           @default(0)
  focus_score  Int            @default(0)
  notes        String?
  created_at   DateTime?      @default(now()) @db.Timestamptz(6)
  updated_at   DateTime?      @db.Timestamptz(6)
  strategy     FocusStrategy?
  status       TaskStatus     @default(PENDING)
  deleted_at   DateTime?      @db.Timestamptz(6)
  User         User           @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_User_study_sessions")
  Subject      Subject?       @relation(fields: [subject_id], references: [id], onUpdate: NoAction, map: "fk_study_sessions_subject_id")

  @@index([user_id, start_time(sort: Desc)], map: "idx_study_session_user_start_active", where: raw("(deleted_at IS NULL)"))
  @@index([user_id, start_time(sort: Desc)], map: "idx_study_session_user_start_safe", where: raw("(deleted_at IS NULL)"))
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SubTopic {
  id               String             @id @db.Uuid
  topic_id         String             @db.Uuid
  title            String             @default("")
  description      String?
  content          String?
  video_url        String?
  order            Int                @default(0)
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?          @db.Timestamptz(6)
  duration_minutes Int                @default(0)
  is_free          Boolean            @default(false)
  type             LessonType         @default(VIDEO)
  exam_id          String?            @db.Uuid
  deleted_at       DateTime?          @db.Timestamptz(6)
  Exam             Exam?              @relation(fields: [exam_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_SubTopic_exam")
  Topic            Topic              @relation(fields: [topic_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_Topic_sub_topics")
  TopicProgress    TopicProgress[]
  LessonAttachment LessonAttachment[]
  LessonNote       LessonNote[]
  LessonQuestion   LessonQuestion[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Subject {
  id                       String                   @id @db.Uuid
  name                     String
  name_ar                  String?
  code                     String?
  description              String?
  icon                     String?
  color                    String?                  @default("#3b82f6")
  type                     SubjectType?
  is_active                Boolean                  @default(true)
  created_at               DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?                @db.Timestamptz(6)
  category_id              String?                  @db.Uuid
  duration_hours           Int?                     @default(0)
  enrolled_count           Int?                     @default(0)
  instructor_id            String?                  @db.Uuid
  instructor_name          String?
  is_published             Boolean                  @default(false)
  learning_objectives      String?
  level                    Level?
  price                    Float?                   @default(0)
  rating                   Float?                   @default(0)
  requirements             String?
  thumbnail_url            String?
  trailer_url              String?
  seo_title                String?
  seo_description          String?
  slug                     String?                  @unique
  completion_rate          Float?                   @default(0)
  course_prerequisites     String[]                 @default([])
  is_featured              Boolean                  @default(false)
  language                 String?                  @default("ar")
  last_content_update      DateTime?
  target_audience          String[]                 @default([])
  video_count              Int?                     @default(0)
  what_you_learn           String[]                 @default([])
  trailer_duration_minutes Int?                     @default(0)
  deleted_at               DateTime?                @db.Timestamptz(6)
  search_vector            Unsupported("tsvector")?
  Book                     Book[]
  Resource                 Resource[]
  OfflineLesson            OfflineLesson[]
  Challenge                Challenge[]
  CourseReview             CourseReview[]
  Exam                     Exam[]
  LiveEvent                LiveEvent[]
  Payment                  Payment[]
  StudySession             StudySession[]
  SubjectEnrollment        SubjectEnrollment[]
  Task                     Task[]
  Topic                    Topic[]
  AiGeneratedContent       AiGeneratedContent[]
  AiGeneratedExam          AiGeneratedExam[]
  LeaderboardEntry         LeaderboardEntry[]
  Schedule                 Schedule[]
  subjectCertificates      SubjectCertificate[]
  subjectReviews           SubjectReview[]
  userGrades               UserGrade[]

  @@index([id], map: "idx_subject_active_rows", where: raw("(deleted_at IS NULL)"))
  @@index([category_id, created_at(sort: Desc)], map: "idx_subject_category_created_active", where: raw("(deleted_at IS NULL)"))
  @@index([created_at(sort: Desc), id], map: "idx_subject_created_active", where: raw("(deleted_at IS NULL)"))
  @@index([created_at(sort: Desc), id], map: "idx_subject_created_active_safe", where: raw("(deleted_at IS NULL)"))
  @@index([is_published, is_active, level, category_id, created_at(sort: Desc)], map: "idx_subject_public_catalog_active", where: raw("(deleted_at IS NULL)"))
  @@index([is_published, is_active, level, category_id, created_at(sort: Desc)], map: "idx_subject_public_catalog_safe", where: raw("(deleted_at IS NULL)"))
  @@index([search_vector], map: "idx_subject_search_vector", type: Gin)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SubjectCertificate {
  id        String   @id @db.Uuid
  subjectId String   @db.Uuid
  userId    String   @db.Uuid
  issuedAt  DateTime @default(now())
  certUrl   String?
  User      User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_SubjectCertificate_user")
  Subject   Subject  @relation(fields: [subjectId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_SubjectCertificate_subject")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SubjectEnrollment {
  id          String    @id @db.Uuid
  user_id     String    @db.Uuid
  subject_id  String    @db.Uuid
  enrolled_at DateTime? @db.Timestamptz(6)
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
  progress    Float?    @default(0)
  deleted_at  DateTime? @db.Timestamptz(6)
  Subject     Subject   @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_Subject_enrollments")
  User        User      @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_User_enrollments")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SubjectReview {
  id        String   @id @db.Uuid
  subjectId String   @db.Uuid
  userId    String   @db.Uuid
  rating    Int
  comment   String?
  createdAt DateTime @default(now())
  updatedAt DateTime
  User      User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_SubjectReview_user")
  Subject   Subject  @relation(fields: [subjectId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_SubjectReview_subject")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SubscriptionPlan {
  id                String              @id @db.Uuid
  name              String
  name_ar           String?
  description       String?
  descriptionAr     String?
  price             Decimal             @db.Decimal
  currency          String              @default("EGP")
  interval          PlanInterval        @default(MONTHLY)
  features          String[]            @default([])
  featuresAr        String[]            @default([])
  is_active         Boolean             @default(true)
  level             Int                 @default(0)
  memberLimit       Int                 @default(1)
  created_at        DateTime            @default(now())
  updated_at        DateTime
  aiMessageLimit    Int?
  examLimit         Int?
  extraExamPrice    Float?              @default(0)
  deleted_at        DateTime?           @db.Timestamptz(6)
  UserSubscription  UserSubscription[]

  @@index([is_active], map: "idx_subscription_plan_is_active")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SupportTicket {
  id                 String    @id @db.Uuid
  ticketNumber       String    @unique @map("ticket_number") @db.VarChar(20)
  userId             String    @map("user_id") @db.Uuid
  userName           String?   @map("user_name") @db.VarChar(200)
  userEmail          String?   @map("user_email") @db.VarChar(255)
  subject            String    @db.VarChar(200)
  description        String    @db.Text
  category           String    @db.VarChar(20)
  status             String?   @default("open") @db.VarChar(20)
  priority           String?   @default("medium") @db.VarChar(10)
  assignedTo         String?   @map("assigned_to") @db.Uuid
  assignedToName     String?   @map("assigned_to_name") @db.VarChar(200)
  tags               String[]  @default([])
  relatedEntityType  String?   @map("related_entity_type") @db.VarChar(50)
  relatedEntityId    String?   @map("related_entity_id") @db.Uuid
  createdAt          DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt          DateTime? @default(now()) @map("updated_at") @db.Timestamptz(6)
  resolvedAt         DateTime? @map("resolved_at") @db.Timestamptz(6)
  closedAt           DateTime? @map("closed_at") @db.Timestamptz(6)
  satisfactionRating Int?      @map("satisfaction_rating")
  deletedAt          DateTime? @map("deleted_at") @db.Timestamptz(6)
  User               User      @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_support_tickets_user_id")

  @@map("support_tickets")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SystemSetting {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  key        String    @unique @db.VarChar(100)
  value      String?
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_at DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at DateTime? @db.Timestamptz(6)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Task {
  id             String        @id @db.Uuid
  user_id        String        @db.Uuid
  title          String
  description    String?
  subject_id     String?       @db.Uuid
  due_at         DateTime?     @db.Timestamptz(6)
  scheduledAt    DateTime?
  completed_at   DateTime?
  priority       TaskPriority?
  created_at     DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at     DateTime?     @db.Timestamptz(6)
  status         TaskStatus    @default(PENDING)
  isDeleted      Boolean       @default(false)
  deletedAt      DateTime?
  estimated_time BigInt?
  actual_time    BigInt?
  deleted_at     DateTime?     @db.Timestamptz(6)
  Subject        Subject?      @relation(fields: [subject_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_Task_subject")
  User           User          @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_User_tasks")

  @@index([user_id, status, created_at(sort: Desc)], map: "idx_task_user_status_active_created", where: raw("(deleted_at IS NULL)"))
  @@index([user_id, status, created_at(sort: Desc)], map: "idx_task_user_status_created_safe", where: raw("(deleted_at IS NULL)"))
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Teacher {
  id              String           @id @db.Uuid
  userId          String           @unique @db.Uuid
  name            String
  bio             String?
  image           String?
  onlineUrl       String?
  rating          Float            @default(0.0)
  notes           String?
  createdAt       DateTime         @default(now())
  updatedAt       DateTime
  User            User             @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_Teacher_user")
  TeacherEarnings TeacherEarnings?
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model TeacherEarnings {
  id              String   @id @db.Uuid
  teacherId       String   @unique @db.Uuid
  balance         Decimal  @default(0) @db.Decimal
  totalEarned     Decimal  @default(0) @db.Decimal
  withdrawnAmount Decimal  @default(0) @db.Decimal
  currency        String   @default("EGP")
  updatedAt       DateTime
  Teacher         Teacher  @relation(fields: [teacherId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_TeacherEarnings_teacher")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model TestResult {
  id          String   @id @db.Uuid
  userId      String   @db.Uuid
  examId      String   @db.Uuid
  score       Float
  totalScore  Float
  answers     Json?
  timeTaken   Int
  completedAt DateTime @default(now())
  createdAt   DateTime @default(now())
  updatedAt   DateTime
  User        User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_TestResult_user")
  Exam        Exam     @relation(fields: [examId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_TestResult_exam")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Topic {
  id          String     @id @db.Uuid
  subject_id  String     @db.Uuid
  title       String     @default("")
  description String?
  order       Int        @default(0)
  created_at  DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at  DateTime?  @db.Timestamptz(6)
  deleted_at  DateTime?  @db.Timestamptz(6)
  SubTopic    SubTopic[]
  Subject     Subject    @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_Subject_topics")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model TopicProgress {
  id                    String    @id @db.Uuid
  user_id               String    @db.Uuid
  sub_topic_id          String    @db.Uuid
  completed             Boolean   @default(false)
  completed_at          DateTime?
  lastVideoPosition     Int       @default(0)
  created_at            DateTime? @default(now()) @db.Timestamptz(6)
  updated_at            DateTime? @db.Timestamptz(6)
  deleted_at            DateTime? @db.Timestamptz(6)
  status                String?   @default("NOT_STARTED")
  time_spent_seconds    BigInt?   @default(0)
  last_watched_position BigInt?   @default(0)
  User                  User      @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_User_lesson_progresses")
  SubTopic              SubTopic  @relation(fields: [sub_topic_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_topic_progress_subtopic")

  @@unique([user_id, sub_topic_id], map: "unique_user_lesson")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model TwoFactorChallenge {
  id        String   @id @db.Uuid
  userId    String?  @db.Uuid
  code      String
  expiresAt DateTime
  used      Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime
  User      User?    @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_TwoFactorChallenge_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model TwoFactorSetting {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  userId          String    @unique @map("user_id") @db.Uuid
  method          String?   @db.VarChar(20)
  secret          String?   @db.VarChar(100)
  isEnabled       Boolean?  @default(false) @map("is_enabled")
  isEnforced      Boolean?  @default(false) @map("is_enforced")
  backupCodes     String[]  @default([]) @map("backup_codes")
  verifiedDevices String[]  @default([]) @map("verified_devices")
  pendingSetup    Boolean?  @default(false) @map("pending_setup")
  activatedAt     DateTime? @map("activated_at") @db.Timestamptz(6)
  deactivatedAt   DateTime? @map("deactivated_at") @db.Timestamptz(6)
  lastUsedAt      DateTime? @map("last_used_at") @db.Timestamptz(6)
  createdAt       DateTime? @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime? @default(now()) @map("updated_at") @db.Timestamptz(6)
  deletedAt       DateTime? @map("deleted_at") @db.Timestamptz(6)
  User            User      @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_two_factor_settings_user_id")

  @@index([userId], map: "idx_two_factor_settings_user_id", where: raw("(deleted_at IS NULL)"))
  @@map("two_factor_settings")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model User {
  id                             String                @id @db.Uuid
  email                          String                @unique
  name                           String?
  username                       String?
  avatar                         String?
  created_at                     DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at                     DateTime?             @db.Timestamptz(6)
  wakeUpTime                     String?               @map("wake_up_time")
  sleepTime                      String?               @map("sleep_time")
  focusStrategy                  String?               @default("POMODORO") @map("focus_strategy")
  emailNotifications             Boolean?              @default(true) @map("email_notifications")
  emailVerificationToken         String?               @map("email_verification_token")
  emailVerificationExpires       DateTime?             @map("email_verification_expires")
  emailVerified                  Boolean?              @default(false) @map("email_verified")
  lastLogin                      DateTime?             @map("last_login")
  phone                          String?
  phoneVerified                  Boolean?              @default(false) @map("phone_verified")
  phoneVerificationOtp           String?               @map("phone_verification_otp")
  phoneVerificationExpires       DateTime?             @map("phone_verification_expires")
  phoneVerificationAttempts      Int                   @default(0) @map("phone_verification_attempts")
  phoneVerificationLastSent      DateTime?             @map("phone_verification_last_sent")
  resetToken                     String?               @map("reset_token")
  resetTokenExpires              DateTime?             @map("reset_token_expires")
  recoveryCodes                  Json?                 @map("recovery_codes")
  smsNotifications               Boolean?              @default(false) @map("sms_notifications")
  biometricEnabled               Boolean               @default(false) @map("biometric_enabled")
  magicLinkToken                 String?               @map("magic_link_token")
  magicLinkExpires               DateTime?             @map("magic_link_expires")
  googleId                       String?               @map("google_id")
  githubId                       String?               @map("github_id")
  passwordChangedAt              DateTime?             @map("password_changed_at")
  passwordExpiresAt              DateTime?             @map("password_expires_at")
  passwordExpirationWarningSent  Boolean               @default(false) @map("password_expiration_warning_sent")
  role                           UserRole              @default(STUDENT)
  status                         UserStatus            @default(ACTIVE)
  country                        String?
  dateOfBirth                    DateTime?             @map("date_of_birth")
  gender                         String?
  alternativePhone               String?               @map("alternative_phone")
  section                        String?
  interestedSubjects             String[]              @default([]) @map("interested_subjects")
  studyGoal                      String?               @map("study_goal")
  subjectsTaught                 String[]              @default([]) @map("subjects_taught")
  classesTaught                  String[]              @default([]) @map("classes_taught")
  experienceYears                String?               @map("experience_years")
  bio                            String?
  permissions                    Json?                 @default("[]")
  school                         String?
  referralCode                   String?               @map("referral_code")
  referredById                   String?               @map("referred_by_id") @db.Uuid
  additionalAiCredits            Int                   @default(0) @map("additional_ai_credits")
  additionalExamCredits          Int                   @default(0) @map("additional_exam_credits")
  lastUsageReset                 DateTime              @default(now()) @map("last_usage_reset")
  monthlyAiMessageCount          Int                   @default(0) @map("monthly_ai_message_count")
  monthlyExamCount               Int                   @default(0) @map("monthly_exam_count")
  totalXp                        BigInt?               @default(0) @map("total_xp")
  level                          BigInt?               @default(1)
  balance                        Decimal?              @default(0) @db.Decimal
  aiCredits                      BigInt?               @default(0) @map("ai_credits")
  examCredits                    BigInt?               @default(0) @map("exam_credits")
  resetPasswordToken             String?               @map("reset_password_token")
  resetPasswordExpires           DateTime?             @map("reset_password_expires") @db.Timestamptz(6)
  verificationToken              String?               @map("verification_token")
  verificationExpires            DateTime?             @map("verification_expires") @db.Timestamptz(6)
  deletedAt                      DateTime?             @map("deleted_at") @db.Timestamptz(6)
  currentStreak                  Int                   @default(0) @map("current_streak")
  longestStreak                  Int                   @default(0) @map("longest_streak")
  totalStudyTime                 Int                   @default(0) @map("total_study_time")
  tasksCompleted                 Int                   @default(0) @map("tasks_completed")
  examsPassed                    Int                   @default(0) @map("exams_passed")
  studyXp                        Int                   @default(0) @map("study_xp")
  taskXp                         Int                   @default(0) @map("task_xp")
  examXp                         Int                   @default(0) @map("exam_xp")
  challengeXp                    Int                   @default(0) @map("challenge_xp")
  questXp                        Int                   @default(0) @map("quest_xp")
  seasonXp                       Int                   @default(0) @map("season_xp")
  passwordHash                   String                @map("password_hash")
  gradeLevel                     String?               @map("grade_level")
  educationType                  String?               @map("education_type")
  version                        BigInt?               @default(1)
  activeSubscriptionId           String?               @map("active_subscription_id") @db.Uuid
  subscriptionExpiresAt          DateTime?             @map("subscription_expires_at") @db.Timestamptz(6)
  twoFactorEnabled               Boolean?              @default(false) @map("two_factor_enabled")
  twoFactorSecret                String?               @map("two_factor_secret")
  archiveReason                  String?               @map("archive_reason")
  clerkId                        String?               @unique(map: "idx_user_clerk_id") @map("clerk_id") @db.VarChar(255)
  OfflineLesson_User             OfflineLesson[]       @relation("UserOfflineLessons")
  OfflineLesson_Teacher          OfflineLesson[]       @relation("TeacherOfflineLessons")
  BlogPost                       BlogPost[]
  CourseReview                   CourseReview[]
  EventAttendee                  EventAttendee[]
  ExamResult                     ExamResult[]
  ForumTopic                     ForumTopic[]
  Invoice                        Invoice[]
  Lesson_Lesson_teacher_idToUser Lesson[]              @relation("Lesson_teacher_idToUser")
  Lesson_Lesson_user_idToUser    Lesson[]              @relation("Lesson_user_idToUser")
  Notification                   Notification[]
  Payment                        Payment[]
  Reminder                       Reminder[]
  Schedule                       Schedule[]
  SecurityLog                    SecurityLog[]
  StudySession                   StudySession[]
  SubjectEnrollment              SubjectEnrollment[]
  Task                           Task[]
  TopicProgress                  TopicProgress[]
  User                           User?                 @relation("UserToUser", fields: [referredById], references: [id], onUpdate: NoAction, map: "fk_user_referred_by_id")
  other_User                     User[]                @relation("UserToUser")
  UserAchievement                UserAchievement[]
  UserChallenge                  UserChallenge[]
  UserSession                    UserSession[]
  UserSettings                   UserSettings?
  UserSubscription               UserSubscription[]
  WalletTransaction              WalletTransaction[]
  Announcement                   Announcement[]
  AuditLog                       AuditLog[]
  AutomationLog                  AutomationLog[]
  BookProgress                   BookProgress[]
  BookReview                     BookReview[]
  ChallengeCompletion            ChallengeCompletion[]
  ContentPreference              ContentPreference[]
  ContentReport_Reporter         ContentReport[]       @relation("ContentReport_reporter")
  ContentReport_Resolver         ContentReport[]       @relation("ContentReport_resolver")
  CustomGoal                     CustomGoal[]
  Event_Organizer                Event[]
  ForumPost_Author               ForumPost[]           @relation("ForumPost_author")
  ForumReply_Author              ForumReply[]          @relation("ForumReply_author")
  LeaderboardEntry               LeaderboardEntry[]
  LessonAnswer                   LessonAnswer[]
  LessonNote                     LessonNote[]
  LessonQuestion                 LessonQuestion[]
  Message_Sender                 Message[]             @relation("Message_sender")
  Message_Receiver               Message[]             @relation("Message_receiver")
  MlRecommendation               MlRecommendation[]
  PasswordHistory                PasswordHistory[]
  ProgressSnapshot               ProgressSnapshot[]
  QuestProgress                  QuestProgress[]
  ReferralReward_Referrer        ReferralReward[]      @relation("ReferralReward_referrer")
  ReferralReward_Referred        ReferralReward[]      @relation("ReferralReward_referred")
  SecurityQuestion               SecurityQuestion[]
  SentimentAnalysis              SentimentAnalysis[]
  SubjectCertificate             SubjectCertificate[]
  SubjectReview                  SubjectReview[]
  TestResult                     TestResult[]
  TwoFactorChallenge             TwoFactorChallenge[]
  UserActivity                   UserActivity?
  UserGrade                      UserGrade[]
  UserInteraction                UserInteraction[]
  UserReward                     UserReward[]
  UserWallet                     UserWallet?
  UserXP                         UserXP?
  AiChatMessage                  AiChatMessage[]
  AiGeneratedContent             AiGeneratedContent[]
  AiGeneratedExam                AiGeneratedExam[]
  AnalyticsEvent                 AnalyticsEvent[]
  BiometricChallenge             BiometricChallenge[]
  BiometricCredential            BiometricCredential[]
  Contest_Organizer              Contest[]             @relation("Contest_organizer")
  Coupon                         Coupon[]
  SeasonParticipation            SeasonParticipation[]
  AiQuestion                     AiQuestion[]
  PushToken                      PushToken[]
  SupportTicket                  SupportTicket[]
  TwoFactorSetting               TwoFactorSetting?
  SecurityAuditLog               SecurityAuditLog[]
  teacher                        Teacher?

  @@index([id], map: "idx_user_pkey_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model UserAchievement {
  id             String      @id @db.Uuid
  user_id        String      @db.Uuid
  achievementKey String
  earnedAt       DateTime    @default(now())
  created_at     DateTime    @default(now())
  updated_at     DateTime
  deleted_at     DateTime?   @db.Timestamptz(6)
  achievement_id String      @db.Uuid
  unlocked_at    DateTime?   @db.Timestamptz(6)
  Achievement    Achievement @relation(fields: [achievement_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_UserAchievement_achievement")
  User           User        @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_UserAchievement_user")

  @@index([user_id, unlocked_at(sort: Desc), achievementKey], map: "idx_achievement_user_unlocked_active", where: raw("(deleted_at IS NULL)"))
  @@index([user_id, unlocked_at(sort: Desc)], map: "idx_user_achievement_user_unlocked_safe", where: raw("(deleted_at IS NULL)"))
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model UserActivity {
  userId           String   @id @db.Uuid
  currentStreak    Int      @default(0)
  longestStreak    Int      @default(0)
  totalStudyTime   Int      @default(0)
  tasksCompleted   Int      @default(0)
  examsPassed      Int      @default(0)
  pomodoroSessions Int      @default(0)
  deepWorkSessions Int      @default(0)
  lastActiveAt     DateTime @default(now())
  updatedAt        DateTime
  User             User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_UserActivity_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model UserChallenge {
  id           String    @id @db.Uuid
  user_id      String    @db.Uuid
  challenge_id String    @db.Uuid
  progress     BigInt?   @default(0)
  is_completed Boolean?  @default(false)
  completed_at DateTime? @db.Timestamptz(6)
  deleted_at   DateTime? @db.Timestamptz(6)
  Challenge    Challenge @relation(fields: [challenge_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_UserChallenge_challenge")
  User         User      @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_UserChallenge_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model UserGrade {
  id        String    @id @db.Uuid
  userId    String    @db.Uuid
  subjectId String    @db.Uuid
  grade     Float
  maxGrade  Float
  examName  String?
  date      DateTime?
  examDate  DateTime?
  createdAt DateTime  @default(now())
  updatedAt DateTime
  User      User      @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_UserGrade_user")
  Subject   Subject   @relation(fields: [subjectId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_UserGrade_subject")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model UserInteraction {
  id        String   @id @db.Uuid
  userId    String   @db.Uuid
  type      String
  itemType  String
  itemId    String
  metadata  Json?
  timestamp DateTime @default(now())
  createdAt DateTime @default(now())
  updatedAt DateTime
  User      User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_UserInteraction_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model UserReward {
  id         String   @id @db.Uuid
  userId     String   @db.Uuid
  rewardId   String   @db.Uuid
  earnedAt   DateTime @default(now())
  source     String?
  nftTokenId String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime
  User       User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_UserReward_user")
  Reward     Reward   @relation(fields: [rewardId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_UserReward_reward")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model UserSession {
  id                 String    @id @db.Uuid
  user_id            String    @db.Uuid
  refresh_token      String    @unique(map: "idx_usersession_refresh_token")
  user_agent         String?
  ip                 String
  location           String?
  device_type        String?
  status             String?   @default("active")
  is_active          Boolean?  @default(true)
  last_accessed      DateTime? @db.Timestamptz(6)
  expires_at         DateTime? @db.Timestamptz(6)
  revoked_at         DateTime? @db.Timestamptz(6)
  revoked_by         String?   @db.Uuid
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  updated_at         DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at         DateTime? @db.Timestamptz(6)
  refresh_token_hash String?   @unique(map: "idx_user_session_refresh_hash", where: raw("(refresh_token_hash IS NOT NULL)"))
  User               User      @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_User_Sessions")

  @@index([id, refresh_token_hash, is_active], map: "idx_user_session_id_hash_active")
  @@index([user_id, is_active, last_accessed(sort: Desc)], map: "idx_user_session_user_active_recent", where: raw("(deleted_at IS NULL)"))
  @@index([user_id, is_active, last_accessed(sort: Desc)], map: "idx_user_session_user_active_recent_safe", where: raw("(deleted_at IS NULL)"))
  @@index([id, refresh_token, is_active], map: "idx_user_sessions_active_refresh_token", where: raw("((is_active = true) AND (deleted_at IS NULL))"))
  @@index([user_id, is_active, last_accessed(sort: Desc)], map: "idx_user_sessions_user_active_recent", where: raw("(deleted_at IS NULL)"))
  @@index([deleted_at], map: "idx_usersession_deleted_at")
  @@index([expires_at], map: "idx_usersession_expires_at")
  @@index([is_active], map: "idx_usersession_is_active")
  @@index([user_id], map: "idx_usersession_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model UserSettings {
  id                   String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  theme                String?   @default("light")
  language             String?   @default("ar")
  user_id              String    @unique(map: "unique_user_settings") @db.Uuid
  createdAt            DateTime? @map("created_at") @db.Timestamptz(6)
  updatedAt            DateTime? @map("updated_at") @db.Timestamptz(6)
  deletedAt            DateTime? @map("deleted_at") @db.Timestamptz(6)
  profileVisibility    String?   @default("public") @map("profile_visibility")
  showOnlineStatus     Boolean?  @default(true) @map("show_online_status")
  showProgress         Boolean?  @default(true) @map("show_progress")
  quietHoursEnabled    Boolean?  @default(false) @map("quiet_hours_enabled")
  quietHoursStart      String?   @default("22:00") @map("quiet_hours_start")
  quietHoursEnd        String?   @default("07:00") @map("quiet_hours_end")
  soundEnabled         Boolean?  @default(true) @map("sound_enabled")
  vibrationEnabled     Boolean?  @default(true) @map("vibration_enabled")
  fontSize             String?   @default("medium") @map("font_size")
  reducedMotion        Boolean?  @default(false) @map("reduced_motion")
  highContrast         Boolean?  @default(false) @map("high_contrast")
  compactMode          Boolean?  @default(false) @map("compact_mode")
  efficiencyMode       Boolean?  @default(false) @map("efficiency_mode")
  numberFormat         String?   @default("english") @map("number_format")
  notificationsEnabled Boolean?  @default(true) @map("notifications_enabled")
  studyReminders       Boolean?  @default(true) @map("study_reminders")
  emailNotifications   Boolean?  @default(true) @map("email_notifications")
  pushNotifications    Boolean?  @default(true) @map("push_notifications")
  taskReminders        Boolean?  @default(true) @map("task_reminders")
  taskReminderTime     String?   @default("30") @map("task_reminder_time")
  dailyGoalReminders   Boolean?  @default(true) @map("daily_goal_reminders")
  examReminders        Boolean?  @default(true) @map("exam_reminders")
  examReminderDays     BigInt?   @default(3) @map("exam_reminder_days")
  deadlineReminders    Boolean?  @default(true) @map("deadline_reminders")
  progressReports      Boolean?  @default(true) @map("progress_reports")
  weeklyReport         Boolean?  @default(true) @map("weekly_report")
  achievementAlerts    Boolean?  @default(true) @map("achievement_alerts")
  commentNotifications Boolean?  @default(true) @map("comment_notifications")
  mentionNotifications Boolean?  @default(true) @map("mention_notifications")
  pushEnabled          Boolean?  @default(true) @map("push_enabled")
  emailEnabled         Boolean?  @default(true) @map("email_enabled")
  smsEnabled           Boolean?  @default(false) @map("sms_enabled")
  User                 User      @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_User_settings")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model UserSubscription {
  id                     String           @id @db.Uuid
  user_id                String           @db.Uuid
  plan_id                String           @db.Uuid
  status                 String           @default("PENDING")
  start_date             DateTime         @db.Timestamptz(6)
  end_date               DateTime         @db.Timestamptz(6)
  auto_renew             Boolean          @default(true)
  paymob_subscription_id String?
  created_at             DateTime         @default(now()) @db.Timestamptz(6)
  updated_at             DateTime         @default(now()) @db.Timestamptz(6)
  SubscriptionPlan       SubscriptionPlan @relation(fields: [plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "user_subscription_plan_fk")
  User                   User             @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "user_subscription_user_fk")

  @@index([user_id, status, end_date], map: "idx_user_subscription_active_lookup")
  @@index([end_date], map: "idx_user_subscription_end_date")
  @@index([paymob_subscription_id], map: "idx_user_subscription_paymob")
  @@index([plan_id], map: "idx_user_subscription_plan_id")
  @@index([status], map: "idx_user_subscription_status")
  @@index([user_id], map: "idx_user_subscription_user_id")
  @@index([user_id, status, end_date(sort: Desc)], map: "idx_user_subscription_user_status_end")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model UserWallet {
  userId            String              @id @db.Uuid
  balance           Decimal             @default(0) @db.Decimal
  updatedAt         DateTime
  currency          String              @default("EGP")
  User              User                @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_UserWallet_user")
  WalletTransaction WalletTransaction[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model UserXP {
  userId      String   @id @db.Uuid
  totalXP     Int      @default(0)
  level       Int      @default(1)
  studyXP     Int      @default(0)
  taskXP      Int      @default(0)
  examXP      Int      @default(0)
  challengeXP Int      @default(0)
  questXP     Int      @default(0)
  seasonXP    Int      @default(0)
  updatedAt   DateTime
  User        User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_UserXP_user")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model WalletTransaction {
  id          String                  @id @db.Uuid
  walletId    String                  @db.Uuid
  user_id     String                  @db.Uuid
  amount      Decimal                 @db.Decimal
  type        WalletTransactionType
  status      WalletTransactionStatus @default(COMPLETED)
  description String?
  paymentId   String?
  metadata    Json?
  created_at  DateTime                @default(now()) @db.Timestamptz(6)
  updatedAt   DateTime
  deleted_at  DateTime?               @db.Timestamptz(6)
  Wallet      UserWallet              @relation(fields: [walletId], references: [userId], onDelete: Cascade, onUpdate: NoAction, map: "fk_wallet_transactions_wallet")
  User        User                    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_wallet_transactions_user_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model GroupMembers {
  A String
  B String

  @@id([A, B], map: "_GroupMembers_AB_pkey")
  @@map("_GroupMembers")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SubjectToTeacher {
  A String
  B String

  @@id([A, B], map: "_SubjectToTeacher_AB_pkey")
  @@map("_SubjectToTeacher")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model blocked_ip_attempts {
  id           String    @id @db.Uuid
  ip_address   String
  endpoint     String?
  method       String?
  user_agent   String?
  location     String?
  reason       String?
  user_id      String?   @db.Uuid
  count        Int?      @default(1)
  attempted_at DateTime? @default(now()) @db.Timestamptz(6)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model broadcasts {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  title         String    @db.VarChar(200)
  message       String
  type          String?   @default("info") @db.VarChar(20)
  channels      Json?     @default("[]")
  target_count  Int?      @default(0)
  success_count Int?      @default(0)
  failure_count Int?      @default(0)
  status        String?   @default("draft") @db.VarChar(20)
  scheduled_for DateTime? @db.Timestamptz(6)
  sent_at       DateTime? @db.Timestamptz(6)
  created_by    String    @db.Uuid
  cancelled_by  String?   @db.Uuid
  cancelled_at  DateTime? @db.Timestamptz(6)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  updated_at    DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at    DateTime? @db.Timestamptz(6)

  @@index([deleted_at], map: "idx_broadcasts_deleted_at")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model deleted_record_archive {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  table_name String   @db.VarChar(255)
  record_id  String
  user_id    String?
  data       Json
  reason     String?  @db.VarChar(500)
  createdAt  DateTime @default(now()) @db.Timestamptz(6)
  updatedAt  DateTime @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ip_whitelist_settings {
  id                   String    @id @db.Uuid
  is_enabled           Boolean?  @default(false)
  enforce_for_admins   Boolean?  @default(true)
  enforce_for_api      Boolean?  @default(false)
  default_action       String?   @default("allow")
  allow_internal_ips   Boolean?  @default(true)
  internal_ip_ranges   String[]
  log_blocked_attempts Boolean?  @default(true)
  notify_on_violation  Boolean?  @default(true)
  notify_email         String?
  created_at           DateTime? @default(now()) @db.Timestamptz(6)
  updated_at           DateTime? @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schema_migrations {
  id        String   @id
  checksum  String
  appliedAt DateTime @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_journey_steps {
  id         String    @id @db.Uuid
  journey_id String    @db.Uuid
  user_id    String    @db.Uuid
  session_id String    @db.VarChar(100)
  page       String    @db.VarChar(500)
  action     String    @db.VarChar(100)
  metadata   Json?
  timestamp  DateTime  @db.Timestamptz(6)
  duration   BigInt?
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at DateTime? @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model scheduled_items {
  id            String    @id @db.Uuid
  type          String    @db.VarChar(20)
  title         String    @db.VarChar(200)
  description   String?   @db.Text
  content       Json?
  scheduled_for DateTime  @db.Timestamptz(6)
  timezone      String?   @default("UTC") @db.VarChar(50)
  frequency     String?   @default("once") @db.VarChar(20)
  status        String?   @default("pending") @db.VarChar(20)
  retry_count   Int?      @default(0)
  max_retries   Int?      @default(3)
  error         String?   @db.Text
  executed_at   DateTime? @db.Timestamptz(6)
  cancelled_at  DateTime? @db.Timestamptz(6)
  created_by    String    @db.Uuid
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  updated_at    DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at    DateTime? @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model backups {
  id                String    @id @db.Uuid
  name              String    @db.VarChar(100)
  type              String    @db.VarChar(20)
  size              BigInt?
  status            String?   @default("pending") @db.VarChar(20)
  checksum          String?   @db.VarChar(64)
  download_url      String?   @db.VarChar(500)
  includes_files    Boolean?  @default(false)
  includes_database Boolean?  @default(false)
  tables            String[]  @default([])
  retention_days    Int?      @default(30)
  created_by        String    @db.Uuid
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
  completed_at      DateTime? @db.Timestamptz(6)
  error             String?   @db.Text
  restored_at       DateTime? @db.Timestamptz(6)
  restored_by       String?   @db.Uuid
  deleted_at        DateTime? @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_flows {
  id         String    @id @db.Uuid
  from_page  String    @db.VarChar(500)
  to_page    String    @db.VarChar(500)
  date       DateTime  @db.Date
  count      BigInt?   @default(0)
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_at DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at DateTime? @db.Timestamptz(6)

  @@unique([from_page, to_page, date], map: "idx_flow")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Campaign {
  id          String    @id @db.Uuid
  name        String    @db.VarChar(255)
  description String?   @db.Text
  type        String?   @default("email") @db.VarChar(50)
  status      String?   @default("DRAFT") @db.VarChar(50)
  target_role String?   @db.VarChar(50)
  content     String?   @db.Text
  start_date  DateTime? @db.Timestamptz(6)
  end_date    DateTime? @db.Timestamptz(6)
  sent_count  Int?      @default(0)
  open_count  Int?      @default(0)
  click_count Int?      @default(0)
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  updated_at  DateTime? @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ticket_messages {
  id          String    @id @db.Uuid
  ticket_id   String    @db.Uuid
  sender_id   String    @db.Uuid
  sender_name String    @db.VarChar(200)
  sender_role String    @db.VarChar(20)
  message     String    @db.Text
  attachments Json?
  is_internal Boolean?  @default(false)
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  read_at     DateTime? @db.Timestamptz(6)
  deleted_at  DateTime? @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_journeys {
  id              String    @id @db.Uuid
  user_id         String    @db.Uuid
  session_id      String    @db.VarChar(100)
  started_at      DateTime  @db.Timestamptz(6)
  ended_at        DateTime? @db.Timestamptz(6)
  total_duration  BigInt?
  conversion_goal String?   @db.VarChar(100)
  completed       Boolean?  @default(false)
  device_info     String?   @db.VarChar(500)
  ip_address      String?   @db.VarChar(50)
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  updated_at      DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at      DateTime? @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model page_views {
  id              String    @id @db.Uuid
  page            String    @db.VarChar(500)
  date            DateTime  @db.Date
  views           BigInt?   @default(0)
  unique_visitors BigInt?   @default(0)
  avg_duration    Float?
  bounces         BigInt?   @default(0)
  exits           BigInt?   @default(0)
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  updated_at      DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at      DateTime? @db.Timestamptz(6)

  @@unique([page, date], map: "idx_page_date")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model conversion_events {
  id            String    @id @db.Uuid
  user_id       String    @db.Uuid
  session_id    String    @db.VarChar(100)
  journey_id    String?   @db.Uuid
  goal          String    @db.VarChar(100)
  value         Float?
  currency      String?   @db.VarChar(3)
  timestamp     DateTime  @db.Timestamptz(6)
  journey_steps Int?
  source        String?   @db.VarChar(100)
  campaign_id   String?   @db.Uuid
  metadata      Json?
  ip_address    String?   @db.VarChar(50)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at    DateTime? @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model active_user_stats {
  id                   String    @id @db.Uuid
  date                 DateTime  @db.Date
  daily_active         BigInt?   @default(0)
  weekly_active        BigInt?   @default(0)
  monthly_active       BigInt?   @default(0)
  new_users            BigInt?   @default(0)
  returning_users      BigInt?   @default(0)
  avg_session_duration Float?
  total_sessions       BigInt?   @default(0)
  created_at           DateTime? @default(now()) @db.Timestamptz(6)
  updated_at           DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([date], map: "idx_active_user_stats_date")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model custom_reports {
  id                 String    @id @db.Uuid
  name               String    @db.VarChar(100)
  description        String?   @db.Text
  widgets            Json?
  filters            Json?
  date_range_from    DateTime? @db.Timestamptz(6)
  date_range_to      DateTime? @db.Timestamptz(6)
  created_by         String    @db.Uuid
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  updated_at         DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at         DateTime? @db.Timestamptz(6)
  is_public          Boolean?  @default(false)
  last_run_at        DateTime? @db.Timestamptz(6)
  schedule_frequency String?   @db.VarChar(20)
  schedule_email_to  String[]  @default([])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ip_whitelist_entries {
  id           String    @id @db.Uuid
  ip_address   String
  cidr         String?
  description  String?
  type         String
  status       String?   @default("active")
  is_temporary Boolean?  @default(false)
  expires_at   DateTime? @db.Timestamptz(6)
  last_used_at DateTime? @db.Timestamptz(6)
  created_by   String    @db.Uuid
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  updated_at   DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at   DateTime? @db.Timestamptz(6)
  country      String?   @db.VarChar(100)
  city         String?   @db.VarChar(100)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Lesson {
  id                           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id                      String    @db.Uuid
  teacher_id                   String    @db.Uuid
  title                        String
  location                     String
  start_time                   DateTime  @db.Timestamptz(6)
  end_time                     DateTime  @db.Timestamptz(6)
  created_at                   DateTime  @default(now()) @db.Timestamptz(6)
  updated_at                   DateTime  @default(now()) @db.Timestamptz(6)
  deleted_at                   DateTime? @db.Timestamptz(6)
  User_Lesson_teacher_idToUser User      @relation("Lesson_teacher_idToUser", fields: [teacher_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  User_Lesson_user_idToUser    User      @relation("Lesson_user_idToUser", fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([end_time], map: "idx_lessons_end")
  @@index([start_time], map: "idx_lessons_start")
  @@index([teacher_id], map: "idx_lessons_teacher")
  @@index([user_id], map: "idx_lessons_user")
  @@index([user_id, start_time], map: "idx_lessons_user_start")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AIConversation {
  id         String      @id @db.Uuid
  user_id    String      @db.Uuid
  subject_id String?     @db.Uuid
  topic_id   String?     @db.Uuid
  title      String?     @db.Text
  is_active  Boolean?    @default(true)
  created_at DateTime?   @default(now()) @db.Timestamptz(6)
  updated_at DateTime?   @default(now()) @db.Timestamptz(6)
  deleted_at DateTime?   @db.Timestamptz(6)
  AIMessage  AIMessage[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AIMessage {
  id              String         @db.Uuid
  conversation_id String         @db.Uuid
  role            String         @db.Text
  content         String         @db.Text
  model           String?        @db.Text
  tokens_used     Int?
  latency         BigInt?
  created_at      DateTime       @db.Timestamptz(6)
  updated_at      DateTime?      @db.Timestamptz(6)
  Conversation    AIConversation @relation(fields: [conversation_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_AIMessage_conversation")

  @@id([id, created_at])
}

// ===========================================================
// ENUMS
// ===========================================================

enum AchievementCategory {
  STUDY
  TASKS
  EXAMS
  TIME
  STREAK
}

enum AddonType {
  EXAM_PACK
  AI_CREDITS
  TEACHER_HOURS
  OTHER
}

enum BlogPostStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}

enum CategoryType {
  BLOG
  FORUM
  COURSE
}

enum ContentReportStatus {
  PENDING
  RESOLVED
  REJECTED
}

enum ContestStatus {
  DRAFT
  WAITING
  IN_PROGRESS
  FINISHED
}

enum Difficulty {
  EASY
  MEDIUM
  HARD
  EXPERT
}

enum DiscountType {
  PERCENTAGE
  FIXED
}

enum ExamType {
  QUIZ
  FINAL
  MIDTERM
  ASSIGNMENT
}

enum FocusStrategy {
  POMODORO
  EIGHTY_TWENTY
  DEEP_WORK
  TIME_BLOCKING
  NO_DISTRACTION
}

enum InvoiceStatus {
  DRAFT
  OPEN
  PAID
  VOID
  UNCOLLECTIBLE
}

enum LessonType {
  VIDEO
  ARTICLE
  QUIZ
  FILE
  ASSIGNMENT
}

enum Level {
  BEGINNER
  INTERMEDIATE
  ADVANCED
}

enum NotificationType {
  INFO
  SUCCESS
  WARNING
  ERROR
}

enum PaymentStatus {
  PENDING
  SUCCESS
  FAILED
  REFUNDED
}

enum PlanInterval {
  MONTHLY
  QUARTERLY
  YEARLY
  LIFETIME
  FOREVER
}

enum SubjectType {
  COURSE
  LESSON
  QUIZ
}

enum SubscriptionStatus {
  ACTIVE
  INACTIVE
  EXPIRED
  CANCELLED
  GRACE_PERIOD
}

enum TaskPriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}

enum TaskStatus {
  PENDING
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum UserRole {
  USER
  STUDENT
  ADMIN
  TEACHER
  MODERATOR
}

enum UserStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
}

enum WalletTransactionStatus {
  PENDING
  COMPLETED
  FAILED
  CANCELLED
}

enum WalletTransactionType {
  DEPOSIT
  WITHDRAWAL
  PAYMENT
  REFUND
  REFERRAL_REWARD
  BONUS
}
