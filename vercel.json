{
  "buildCommand": "go build -buildvcs=false -ldflags=\"-s -w\" -o api/api ./cmd/api",
  "outputDirectory": "api",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
