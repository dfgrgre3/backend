{
  "version": 2,
  "framework": "go",
  "buildCommand": "go build -ldflags '-s -w' -buildvcs=false -o /tmp/api/api ./api",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
