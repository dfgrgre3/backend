{
  "version": 2,
  "framework": "custom",
  "buildCommand": "go build -ldflags '-s -w' -buildvcs=false -o /tmp/api/api ./api",
  "outputDirectory": ".",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}