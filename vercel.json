{
  "version": 2,
  "framework": "go",
  "buildCommand": "go build -ldflags '-s -w' -buildvcs=false -o /tmp/api/api ./cmd/api",
  "outputDirectory": ".",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}